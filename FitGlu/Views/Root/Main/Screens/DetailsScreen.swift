import SwiftUI

struct DetailsScreen: View {
    var body: some View {
        VStack {
            Text("📊 Details Screen")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    DetailsScreen()
}
