import SwiftUI

struct TrainingsScreen: View {
    var body: some View {
        VStack {
            Text("🏋️ Trainings Screen")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    TrainingsScreen()
}
